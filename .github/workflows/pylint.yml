name: Build Python Application

on: [push]

jobs:
  build-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -r requirements.txt

      - name: Build with PyInstaller
        run: |
          pyinstaller --onefile v2iplimit.py

      - name: Rename executable to .bin
        run: |
          mv dist/v2iplimit dist/v2iplimit.bin

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: v2iplimit_amd64_linux.bin
          # path: dist/v2iplimit.bin

  # build-windows:
    # runs-on: windows-latest

    # steps:
      # - name: Checkout repository
        # uses: actions/checkout@v3

      # - name: Set up Python
        # uses: actions/setup-python@v4
        # with:
          # python-version: '3.12'

      # - name: Install dependencies
        # run: |
          # python -m pip install --upgrade pip
          # pip install pyinstaller
          # pip install -r requirements.txt

      # - name: Build with PyInstaller
        # run: |
          # pyinstaller --onefile v2iplimit.py

      # - name: Upload Artifacts
        # uses: actions/upload-artifact@v4
        # with:
          # name: Windows-Build
          # path: dist/v2iplimit.exe